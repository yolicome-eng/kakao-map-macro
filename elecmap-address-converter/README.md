# 전산화번호 주소찾기

전산화번호가 첫 번째 열에 있는 XLSX를 선택하면 ElecMap을 Chrome으로 열어 번호를 순차 검색하고, 검색 응답에서 주소를 찾아 두 번째 열에 기록합니다.

## 실행
1. Windows에서 Chrome 설치
2. GitHub Actions에서 생성된 EXE 실행
3. XLSX 선택
4. 완료 후 `원본파일명_주소결과.xlsx` 확인

현재 버전은 ElecMap의 검색 응답에 주소 정보가 포함되는 경우 이를 우선 추출하고, 화면 텍스트를 보조적으로 확인합니다.
